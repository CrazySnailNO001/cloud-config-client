spring:
  application:
    name: cloud-config-client
  cloud:
    config:
#      uri: http://localhost:8760
      label: master
      profile: local
      discovery:
        enabled: true
        service-id: cloud-config-server
#      enabled: false